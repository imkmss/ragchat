<!--
AUTO-GENERATED by scripts/generate.mjs — do not hand-edit.
Regenerate with: node scripts/generate.mjs --source /path/to/llama.cpp
Extracted on: 2026-07-09
Source files:
- `tools/ui/src/lib/components/ui/utils.ts`
- `tools/ui/src/lib/constants/css-classes.ts`
- `tools/ui/src/lib/components/ui/<each component>/*`
-->
# Component inventory (index)

Every shadcn-svelte primitive under `tools/ui/src/lib/components/ui/` gets its own fully-extracted
reference at `references/components/<name>.md` (verbatim `tv()` variant blocks where a
component defines variants, full source otherwise). Reuse one of these before building a
new primitive from scratch.

| component | variants defined | detail file |
|---|---|---|
| `alert` | alertVariants:variant | `references/components/alert.md` |
| `alert-dialog` | _(no tv() variants)_ | `references/components/alert-dialog.md` |
| `badge` | badgeVariants:variant | `references/components/badge.md` |
| `button` | buttonVariants:variant, buttonVariants:size | `references/components/button.md` |
| `button-group` | _(no tv() variants)_ | `references/components/button-group.md` |
| `card` | _(no tv() variants)_ | `references/components/card.md` |
| `checkbox` | _(no tv() variants)_ | `references/components/checkbox.md` |
| `collapsible` | _(no tv() variants)_ | `references/components/collapsible.md` |
| `dialog` | _(no tv() variants)_ | `references/components/dialog.md` |
| `dropdown-menu` | _(no tv() variants)_ | `references/components/dropdown-menu.md` |
| `input` | _(no tv() variants)_ | `references/components/input.md` |
| `label` | _(no tv() variants)_ | `references/components/label.md` |
| `popover` | _(no tv() variants)_ | `references/components/popover.md` |
| `scroll-area` | _(no tv() variants)_ | `references/components/scroll-area.md` |
| `select` | _(no tv() variants)_ | `references/components/select.md` |
| `separator` | _(no tv() variants)_ | `references/components/separator.md` |
| `sheet` | sheetVariants:side | `references/components/sheet.md` |
| `skeleton` | _(no tv() variants)_ | `references/components/skeleton.md` |
| `switch` | _(no tv() variants)_ | `references/components/switch.md` |
| `table` | _(no tv() variants)_ | `references/components/table.md` |
| `textarea` | _(no tv() variants)_ | `references/components/textarea.md` |
| `tooltip` | _(no tv() variants)_ | `references/components/tooltip.md` |

## Class-merge utility (`tools/ui/src/lib/components/ui/utils.ts`)

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
	return twMerge(clsx(inputs));
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type WithoutChild<T> = T extends { child?: any } ? Omit<T, 'child'> : T;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type WithoutChildren<T> = T extends { children?: any } ? Omit<T, 'children'> : T;
export type WithoutChildrenOrChild<T> = WithoutChildren<WithoutChild<T>>;
export type WithElementRef<T, U extends HTMLElement = HTMLElement> = T & { ref?: U | null };
```

Always merge classes through `cn()` (clsx + tailwind-merge) instead of string concatenation,
so consumer overrides (`class="..."`) win over base variant classes.

## Shared class-string constants (`tools/ui/src/lib/constants/css-classes.ts`)

```ts
export const BOX_BORDER =
	'border border-border/30 focus-within:border-border  dark:border-border/20 dark:focus-within:border-border';

export const INPUT_CLASSES = `
    bg-muted/60 dark:bg-muted/75
    ${BOX_BORDER}
    shadow-sm
    outline-none
    text-foreground
`;

export const PANEL_CLASSES = `
    bg-background
    border border-border/30 dark:border-border/20
    shadow-sm backdrop-blur-lg!
    rounded-t-lg!
`;

export const CHAT_FORM_POPOVER_MAX_HEIGHT = 'max-h-80';
```
